// Stub for codegenNativeComponent – returns a basic component placeholder
export default function codegenNativeComponent(name, component) {
  // Return the component unchanged for web builds
  return component;
}
